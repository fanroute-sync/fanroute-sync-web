# 디자인 시스템 (초안)

출처: `와이어프레임 Rev.3` (2026-07-29, claude.ai 아티팩트)에서 이미 검증된 톤을 그대로 formalize한 것. 새로 만든 방향이 아니라, 와이어프레임 3차 리비전을 거치며 팀이 이미 승인한 팔레트·타이포를 코드에서 바로 쓸 수 있는 토큰으로 정리한 문서.

> **2026-09-12 갱신 — 아래 §2 컬러 절은 실제 구현과 다름.** 이 문서는 테라코타 오렌지 accent를 전제로 작성됐지만, 실제 코드(`globals.css`, 각 화면)는 이 토큰 시스템을 채택한 적이 없고 Tailwind v4 기본 팔레트의 `violet-600`(`#7F22FE`)을 accent로 직접 쓰고 있음. 서비스명 **Troadie** 확정과 함께 "보라 유지"로 컬러 방향도 확정했으므로, 아래 테라코타 표는 **폐기**하고 §2-1(실제 컬러)을 기준으로 삼을 것. `--bg`/`--surface`/`--ink` 등 중성 토큰과 모노스페이스 라벨 방향은 아직 코드에 반영 안 된 상태로, 여전히 유효한 제안으로 남겨둠.

## 1. 방향성

부산 콘서트 여행 앱은 "여행 스펙 문서"에 가까운 신뢰감을 지향한다 — 화려한 이벤트성 UI보다는, 일정·공연 정보를 정확하게 전달하는 실용적인 톤. 구조적 라벨(섹션 번호, eyebrow, caption)에 모노스페이스를 써서 "정리된 계획표" 느낌을 주고, 여기에 accent 하나만 포인트로 써서 공연 열기를 표현한다. 색은 그 외엔 중성 톤(그레이-블루)으로 절제.

## 2. 컬러

### 2-1. 실제 컬러 (확정, 2026-09-12)

Tailwind v4 기본 팔레트를 그대로 사용 — 커스텀 hex 토큰을 새로 정의하지 않고 `violet-*` 유틸리티 클래스를 직접 씀 (`bg-violet-600`, `text-violet-600`, `bg-violet-50` 등 코드 전역에 이미 사용 중).

| 역할 | Tailwind 클래스 | 실측 값 |
|---|---|---|
| accent (버튼, 강조 텍스트, 로고 "ROAD") | `violet-600` | `#7F22FE` |
| accent 배경(배지, 아이콘 배경) | `violet-50` | 옅은 보라 |
| accent 보더/포커스 링 | `violet-500` | — |

로고: 워드마크(T**ROAD**IE, "ROAD"만 `violet-600`)와 앱 아이콘(둥근 사각 배지 + `violet-600` 배경 + 흰색 route 아이콘, `src/app/icon.svg`)으로 확정. `src/components/common/wordmark.tsx` 참고.

### 2-2. 아래는 폐기된 초안 (참고용으로만 남김)

아래 라이트/다크 테라코타 토큰 표는 실제로 구현되지 않았고, 위 2-1로 대체됨. `--bg`/`--surface`/`--ink` 등 중성 톤 자체는 여전히 참고할 만하나, `--accent`만 `violet-600`(`#7F22FE`)으로 바꿔 읽을 것.

### 라이트 (폐기)

| 토큰 | 값 | 용도 |
|---|---|---|
| `--bg` | `#EEF0F2` | 페이지 배경 |
| `--surface` | `#FFFFFF` | 카드/폰/입력 배경 |
| `--surface-2` | `#F5F6F8` | 보조 배경(빈 상태, 플레이스홀더 블록) |
| `--ink` | `#22262B` | 본문 텍스트 |
| `--muted` | `#5B6169` | 보조 텍스트, 라벨 |
| `--line` | `#B8BFC7` | 강조 보더 |
| `--line-soft` | `#DEE2E6` | 일반 구분선 |
| ~~`--accent`~~ | ~~`#D9552A`~~ | **폐기 — `violet-600`(`#7F22FE`)로 대체** |
| ~~`--accent-soft`~~ | ~~`#FAE3D6`~~ | **폐기 — `violet-50`으로 대체** |

### 다크 (폐기)

| 토큰 | 값 |
|---|---|
| `--bg` | `#17191C` |
| `--surface` | `#1E2126` |
| `--surface-2` | `#262A30` |
| `--ink` | `#E8EAED` |
| `--muted` | `#9AA1AA` |
| `--line` | `#4A515A` |
| ~~`--accent`~~ | **폐기 — 다크모드 보라 톤은 아직 미정 (다크모드 자체가 코드에 아직 없음)** |
| ~~`--accent-soft`~~ | **폐기** |

### 접근성 검증 (WCAG 2.1, 실측)

| 조합 | 대비 | 판정 |
|---|---|---|
| `ink` on `surface` (라이트) | 15.2:1 | AAA — 본문 텍스트 안전 |
| `muted` on `surface` (라이트) | 6.3:1 | AA — 보조 텍스트 안전 |
| `accent` as 텍스트 on `surface` (라이트) | 4.0:1 | **큰 텍스트(18px+ bold)에만 사용**, 작은 본문엔 부족 |
| 흰 텍스트 on `accent` 배경 (라이트, solid 버튼) | 4.0:1 | 경계선 — 버튼 라벨은 14px 이상 semibold 권장 |
| `ink` on `bg` (다크) | 14.6:1 | AAA |
| `accent` as 텍스트 on `surface` (다크) | 6.2:1 | AA — 본문 크기도 안전 |
| **흰 텍스트 on `accent` 배경 (다크, solid 버튼)** | **2.6:1** | **❌ 실패.** 다크모드 accent는 밝은 톤이라 흰 글자가 묻힘 |
| `bg`(다크) 텍스트 on `accent` 배경 (다크) | 6.7:1 | ✅ 대안 — solid 버튼은 다크모드에서 **어두운 텍스트**를 써야 함 |

**규칙:** solid 버튼(`accent` 배경 + 텍스트)은 라이트에서는 흰 텍스트, **다크에서는 어두운(`--bg`) 텍스트**를 쓴다. 테마별로 색만 바뀌는 게 아니라 텍스트 색상 자체가 바뀌어야 함 — 컴포넌트 구현 시 반드시 반영.

## 3. 타이포그래피

두 가지 역할만 쓴다 (디스플레이 서체 별도 없음 — 실용적 톤에 맞게 시스템 폰트로 통일):

- **`--font-sans`**: 본문/헤딩 전체. `-apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif`
- **`--font-mono`**: 구조 라벨 전용(섹션 번호, eyebrow, 화면 캡션, 탭 배지). `ui-monospace, "SF Mono", "Cascadia Code", Menlo, Consolas, monospace` — 이게 "스펙 문서" 톤을 만드는 핵심 장치이므로 본문에 섞어 쓰지 않는다.

### 스케일

| 용도 | 크기 | 굵기 | 폰트 |
|---|---|---|---|
| Hero 타이틀 | `clamp(24px, 3.6vw, 34px)` | 700 | sans |
| 섹션 타이틀(h2) | `clamp(20px, 2.2vw, 25px)` | 700 | sans |
| 본문 | 14–15px | 400 | sans |
| 라벨/캡션 | 11.5–12px | 400–600 | mono |
| Eyebrow(섹션 구분자) | 11.5–12px, `letter-spacing: .08em`, uppercase | 400 | mono |

## 4. 스페이싱 & 반경

8px 그리드 기반이 아니라 rem 단위 자유 스케일(와이어프레임 원본 그대로): `.35rem .5rem .75rem 1rem 1.5rem 2rem 2.75rem 3rem`

반경은 요소 성격에 따라 다르게:
- 버튼/카드/입력: `6–8px`
- 큰 컨테이너(폰 프레임 등): `14–30px`
- 칩/뱃지/네비 링크: `999px` (pill)

## 5. 컴포넌트 패턴

| 컴포넌트 | 변형 | 규칙 |
|---|---|---|
| 버튼 | `solid` / `outline` / `accent` / `ghost` / `danger` | `solid`=ink 배경, `accent`=포인트 강조 액션에만 (화면당 1개 권장), `ghost`=보조 액션, `danger`=`#C0392B` 계열 |
| 칩 | 기본 / `on`(선택됨, ink 배경) / `accent-on`(포인트 강조) | 필터·토글·태그에 사용, pill 반경 |
| 카드 | 기본 / `pinned`(accent 보더 + accent-soft 배경) | `pinned`은 화면당 1개로 제한 (D-day, 공연일 고정 카드 등 "지금 가장 중요한 것" 표시용) |
| 배지 | accent-soft 배경 + accent 텍스트 | D-day, 카운트 등 짧은 수치 강조 |
| 입력 | 단일 스타일 | 포커스 시 `--accent` 아웃라인 (`:focus-visible`) |

## 6. 모션 & 접근성

- `@media (prefers-reduced-motion: reduce)`에서 모든 transition/animation 제거 (와이어프레임에 이미 반영됨, 컴포넌트 구현 시 유지)
- 포커스 인디케이터: `outline: 2px solid var(--accent); outline-offset: 2px` — 브라우저 기본 아웃라인 대체가 아니라 `:focus-visible`에만 적용
- 다크모드는 `prefers-color-scheme`(시스템 설정)과 `[data-theme]`(사용자가 앱 내에서 명시적으로 고른 값) 둘 다 지원, 후자가 우선

## 7. Tailwind v4 연동

**2026-09-12 정정**: 아래는 초안 당시 계획이었고 실제로 `globals.css`에 반영되지 않았음 (`globals.css`엔 `--background`/`--foreground`와 Geist 폰트 변수만 있음). 실제 코드는 `@theme` 커스텀 토큰 대신 Tailwind 기본 팔레트 유틸리티(`violet-600` 등)를 화면마다 직접 씀 — §2-1 참고. 중성 토큰(`--bg`/`--surface`/`--ink` 등)을 도입할지는 여전히 미결.

## 8. 다음 단계

- 이 토큰을 기반으로 실제 버튼/칩/카드 등 공용 컴포넌트 구현 (현재 각 화면이 Tailwind 유틸리티를 직접 씀 — `components/ui`에 일부 공용 컴포넌트 존재, 토큰화는 아직)
- 디스플레이 서체를 별도로 둘지(현재는 Geist 단일 트랙) 팀 논의 — 지금은 "실용적 스펙 문서" 톤을 우선해 의도적으로 생략
- ~~서비스명·로고·컬러 확정~~ — **완료 (Troadie / 보라 유지 / 워드마크+앱아이콘, 2026-09-12)**
